@page
@model Universidad.Pages.cursos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Curso[0].Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Curso[0].Creditos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Curso) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Creditos)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Sigla">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Sigla">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Sigla">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
