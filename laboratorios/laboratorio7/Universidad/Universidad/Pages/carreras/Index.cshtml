@page
@model Universidad.Pages.carreras.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Carrera[0].Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Carrera[0].AnnoCreacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Carrera[0].CodEscuelaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Carrera) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AnnoCreacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodEscuelaNavigation.Codigo)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Codigo">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Codigo">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Codigo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
